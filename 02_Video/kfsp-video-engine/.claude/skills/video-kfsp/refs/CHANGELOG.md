# Nhật ký skill `video-kfsp`

- **06/08/2026 (b)** — Thêm **L107** và **L108** vào `refs/LOI_RENDER.md`, đúc từ vòng cuối video "Cơ hội tiềm năng". **L107:** dựng xong một cú chuyển màn dài 4 khung rồi mới thấy nó chạy trọn *dưới* tấm thẻ đang phủ kín khung; cách chữa là chèn vào bản quay một **mảnh đệm tĩnh dài đúng bằng hiệu số hai mốc**. Luật chung rút ra: trước khi dựng bất kỳ chuyển động ngắn nào, kiểm xem quãng đó có lớp nào phủ lên không — mốc đúng mà bị che thì cũng như chưa làm. **L108:** bản quay có hai ranh giới, và cái hay hỏng việc là chỗ **nội dung đổi** (app tự nhảy sang màn khác) chứ không phải độ dài tệp; mọi chỗ dùng lại clip phải kẹp trong ranh giới nội dung, khe dài hơn đoạn dùng được thì kéo chậm theo phép chia, và dải nhắc lại phải dùng đúng đoạn vừa xem ở thân bài.
- **05/08/2026** — Thêm mục **Đổ bóng cho chiếc máy** vào `refs/MOCKUP_3D.md`: bóng hở góc là sai CÁCH LÀM (`boxShadow` vẽ theo hình chữ nhật của thẻ, thân máy là hình trong ảnh PNG) chứ không sai con số; cách đúng là vẽ bóng bằng chính ảnh máy tô trắng rồi làm nhoè. Rút ra luật chung: **chỉnh mãi một con số mà không hết thì đổi cách làm**. Nguồn: L68 trong `LOI_RENDER.md`.

> Tách khỏi `SKILL.md` ngày 05/08/2026 vì đã chiếm 13 KB (18% tệp) mà **không phải luật để làm việc** — chỉ là ghi chép vì sao luật thành ra như vậy.

**Đọc khi:** cần biết một luật ra đời từ đâu, hoặc trước khi định đổi một luật đã có.
**Ghi thêm khi:** dùng skill phát hiện điều mới. Ghi **mới nhất lên trên**, mỗi dòng nêu rõ *đúc từ video nào*.

---

- 2026-08-05 (f): **Chốt cách chọn góc máy, tách [`refs/GOC_MAY.md`](refs/GOC_MAY.md).** Thanh gạt bản dựng bằng một câu "các góc máy quay điện thoại không đẹp"; đưa 8 lựa chọn thì bị trả "còn thiếu nhiều lắm" hai lần, vì cả 8 đều nằm trên **một trục duy nhất**. Thêm **L53** (🔴🔴 "góc máy" là NĂM trục độc lập — xoay quanh trục dọc · ngả trước sau · nghiêng người · cắt khung và cỡ máy · vị trí và số lượng máy; hỏi theo trục và để Thanh trả lời bằng số ghép; trình bày bằng bảng góc dựng từ CHÍNH chiếc máy và clip thật, không đưa ảnh mockup của người khác để chọn) và **L54** (🔴 nhiều máy phải nằm chung một lớp vẽ, xếp chồng nhiều sân khấu làm máy sau bạc màu; nhãn đặt ở dải dưới đục hẳn, đừng đè lên vật đang soi). Bộ dựng `GocMay.tsx` khai 46 góc, mỗi khung một góc, chép sang dự án mới là chạy. Ref còn giữ bảng quy đổi `posX` và trần `frac` lúc máy nằm ngang cho ba khổ.
- 2026-08-05 (e): **Dựng bản v2 "Cơ hội tiềm năng" theo nhịp đo được — 16,8 giây, 504 khung, 10 nhịp đều 48 khung.** Áp thẳng L48: gỡ hết nhịp thở sin, gỡ cú lia dọc thân máy, gỡ cú xoay khoe lưng; cả video còn **đúng hai cú máy** (xoay ngang vào biểu đồ ở khung 240, xoay về dọc kèm lùi và hé lưng ở khung 384). Thêm **L50** (🔴🔴 nền chuyển sắc ở khối cha + hoạt ảnh ở thẻ con = mất sạch chữ, biên dịch vẫn xanh — cả một nhịp trống trơn), **L51** (🔴 cú máy xoay về dọc mà lớp clip vẫn nằm ngang thì màn app quay 90 độ; một clip dùng hai lần phải khoá theo vị trí trong mảng), **L52** (🔴 khổ ngang phải là bố cục hai cột, máy dời sang phải bằng `posX` gộp vào cú máy sẵn có; máy đứng yên thì bỏ luôn thẻ chú giải bám máy 3D). Hai bản khung đóng tách bằng `defaultProps` một tham số, thân bài 0→432 dùng chung. Phông chốt **Inter** qua `@remotion/google-fonts`, không dùng phông hệ điều hành. 🔴 Bài học quy trình đắt nhất phiên: **soi bằng ảnh tĩnh giữa MỖI nhịp**, một vòng 10 ảnh bắt được 6 lỗi mà biên dịch sạch hoàn toàn không thấy.
- 2026-08-05 (d): **Chẻ nhỏ skill để đỡ tốn oan.** `SKILL.md` từ **72 KB xuống 31 KB**. Tách ba khối chỉ cần tới đúng lúc: **nhật ký** (13 KB → tệp này), **lỗi khi render** (28 KB → `LOI_RENDER.md`, đọc trước Bước 5), **mô tả YouTube** (5 KB → `DESCRIPTION_YOUTUBE.md`, đọc sau khi có `final.mp4`). Thay khối liệt kê ref cũ bằng **bảng chỉ đường xếp theo VIỆC ĐANG LÀM** (theo loại video · theo bước đang chạy · theo câu hỏi riêng) để khỏi phải đọc cả tệp mới biết nên đọc gì. Luật mới: phát hiện điều gì thì ghi vào **đúng ref của chủ đề**, không nhồi ngược vào `SKILL.md`. Cùng ngày: **bỏ skill `mkt-content-script`** vì trùng cửa vào với `vlog-script` (cùng bắt "viết kịch bản", "kịch bản video" — máy không có cách chọn đúng); phần hướng dẫn quay cho người quay ngoài đã có sẵn ở `mkt-ugc-brief`. Bản sao giữ ở `99_Back_Office/Archive/skills_da_bo_20260805/`.
- 2026-08-05 (c): **Viết lại mục HDSD trong QUY ĐỊNH RIÊNG THEO LOẠI VIDEO + tách [`refs/HDSD_THAO_TAC.md`](refs/HDSD_THAO_TAC.md).** Mục cũ mô tả cách làm đã bỏ (vẽ mũi tên đỏ lên ảnh chụp tĩnh). Luật mới: loại này **bắt đầu bằng bản quay màn hình, không bắt đầu bằng kịch bản** — viết lời trước rồi ép bản quay khớp theo là luôn phải quay lại. Ref chứa quy trình 6 bước có cổng duyệt, **yêu cầu gửi Thanh khi xin bản quay** (quay chậm, tắt chấm chạm của hệ điều hành, một mạch không cắt ghép), và bảng soi 8 điểm trước khi trình.
- 2026-08-05 (b): **Dọn tài liệu theo mô hình một thương hiệu.** Bỏ luật brand/nobrand còn sót ở 5 chỗ đang có hiệu lực (câu brand spine · lớp Brand Frame · brand-status theo kênh · đường dẫn import `_shared/` · prompt ảnh Gemini) — mâu thuẫn với chính dòng đã ghi 28/07 rằng mô hình kênh nobrand dừng hẳn. Nay: **logo LUÔN hiện**, cờ `NOBRAND` giữ như di sản và luôn để tắt. Đồng bộ engine `~/Desktop/VIDEO KFSP/CLAUDE.md` (viết lại Section 0 thành "Chọn engine", bỏ nhánh `nobrand/`, bỏ danh xưng CEO). Dự án làm trước 28/07 vẫn còn thư mục `brand/`+`nobrand/` — để nguyên, đó là ghi chép đúng của thời điểm đó.
- 2026-08-05 (a): **Bộ dựng video HƯỚNG DẪN THAO TÁC (kiểu 1)** tại `~/Desktop/VIDEO KFSP/_shared/remotion/tutorial/` — quay màn hình lồng khung máy + dấu ngón tay chạm/vuốt + tự phóng vào vùng đang bấm + chú thích ngắn. Viết bài mới chỉ cần thay bản quay và viết danh sách mốc trong `steps.ts`, không đụng mã. Bản mẫu chạy được: `20260805-hdsd-mau/`. 🔴 Ba lỗi đã vấp, chép trong README của bộ dựng: chấm chạm **trắng biến mất trên nền app trắng** (đổi sang tím thương hiệu), hai chú thích chồng nhau, thẻ chữ tràn khỏi viền máy. Thêm hai bẫy kỹ thuật: cấm `vh/vw` (Studio thu nhỏ bằng transform nên xem thử lệch bản xuất) và cấm `__dirname` trong `remotion.config.ts` (tệp bị gói lại, trỏ vào `@remotion/cli/dist`).
- 2026-08-05: **Đo video mẫu bằng máy + chen Google Flow vào pipeline.** Thêm **L48** (🔴🔴 ba phép đo rẻ để tháo một video tham khảo: mốc đổi nội dung ra nhịp thật · biên thân máy ra chuyển động thật · `volumedetect` ra có tiếng hay không. Bản mẫu cùng thể loại hoá ra **máy đứng tuyệt đối yên** và **im hoàn toàn**, mà vẫn cuốn, nhờ nhịp 1,6 giây — ngược hẳn L27, nên L27 nay đọc kèm L48) và **L49** (🔴 thử ý đồ trên Flow trước khi dựng; ranh giới cứng: giao diện app luôn là clip quay thật). Tách [`refs/GOOGLE_FLOW.md`](refs/GOOGLE_FLOW.md) — đường đi Storyboard Studio từng bước, cách khai chiếc máy làm "nhân vật" và phòng chụp làm "bối cảnh", khuôn mô tả cảnh sẵn dán, hai lỗi Flow hay mắc. Đúc từ phiên "Cơ hội tiềm năng" 05/08: bản 29 giây bị gạt vì lê, đếm nhịp mới ra nguyên nhân.
- 2026-08-04 (f): 🔴 **Đóng gói chiếc máy 3D thành KHUÔN DÙNG LẠI** tại `~/Desktop/VIDEO KFSP/_shared/remotion/iphone17pro/` (`Phone3D.tsx` + `cam3d.ts` + `README.md` có **bảng số đo đầy đủ** + `ref/` ảnh gốc Apple và ảnh lưới đã đo). Lần sau copy hai tệp là xong, **không dựng lại**. Viết lại [`refs/MOCKUP_3D.md`](refs/MOCKUP_3D.md) thành cửa vào khuôn. Thêm **L41–L44**. Gốc rễ của cả bốn: chiếc máy được dựng **theo trí nhớ** thay vì theo ảnh sản phẩm chính hãng, Thanh trả về bốn vòng; đo lại mới thấy bo góc thiếu 40%, Dynamic Island thiếu 44%, bệ camera thiếu 27%. Nền video cũng chuyển sang dùng **đúng tấm vân lụa của bộ cover** (đã xoá dấu sao AI bằng `delogo`). **Mặt trước và bo góc Thanh đã duyệt; mặt sau còn nợ ba chỗ** — xem mục cuối `MOCKUP_3D.md`.
- 2026-08-04 (e): **Chuyển hẳn khung máy sang 3D thật** (Thanh chốt). Thêm **L34–L40**. Chiếc máy nay là khối đùn có bề dày, **mặt sau có cụm camera ba ống kính, đèn nháy và logo KFSP**, cạnh có nút nguồn và hai nút âm lượng — nên thêm được ba cú máy mà bản ảnh phẳng không làm nổi: **lia dọc thân máy** khoe bề dày ở đoạn chuyển sang nằm ngang · **lùi ra hé lộ** ở đoạn kết · **xoay ra mặt sau** khoe logo trước khi vào khung đóng. Thẻ chú giải nay **bám máy trong không gian 3D** (chiếu điểm neo trên màn ra khung hình mỗi khung) thay vì dán phẳng. Video "Cơ hội tiềm năng" từ 26,0 giây lên **29,0 giây**, ba khổ. Mã: `Phone3D.tsx` (sân khấu + chiếc máy + mặt màn ghép canvas) · `cam3d.ts` (quy đổi cỡ máy và phép chiếu) · `Thu3D.tsx` (bản thử quay trọn vòng để soi máy trước khi ghép). Bản khung ảnh phẳng `PhoneMock.tsx` giữ lại làm di sản, không còn dùng.
- 2026-08-04 (d): **Dự án video giới thiệu sản phẩm tách ĐỘC LẬP** — `20260804-tinh-nang-co-hoi-tiem-nang/remotion/` nay có `node_modules` riêng (226MB) + `package.json` khoá phiên bản, **đã cài sẵn `@remotion/three` + `three` + `@react-three/fiber`** để dựng khung máy 3D xoay được mặt sau. Không còn liên kết mềm dùng chung với dự án Wyckoff. Thêm [`refs/GIAY_PHEP.md`](refs/GIAY_PHEP.md) — ✅ Thanh đã mua Remotion 1 chỗ ngồi.
- 2026-08-04 (c): **Bóng đổ khung máy phải bám hình máy (L33)** + tách [`refs/MOCKUP_3D.md`](refs/MOCKUP_3D.md): ba mức khung điện thoại (ảnh PNG phẳng → bộ ảnh nhiều góc Angle/Shots.so/Previewed → **model 3D thật + `@remotion/three`**, có `remotion-dev/remotion-three-gltf-example` là điện thoại 3D gắn video vào màn), 14 kiểu chuyển động mockup của Rotato, và 5 thư viện mẫu video giới thiệu tính năng để tham khảo.
- 2026-08-04 (b): **Video GIỚI THIỆU TÍNH NĂNG — công thức riêng, khác hẳn video dạy mẫu hình.** Cài bộ skill Remotion chính chủ (`npx skills add remotion-dev/skills` → `.agents/skills/remotion-*`, đọc `remotion-markup/` trước khi viết mã: `interpolate` nội tuyến trong `style`, `Easing.bezier`, dùng thuộc tính `scale`/`rotate`/`translate` riêng lẻ chứ không nhét chuỗi `transform`). Thêm **L27–L32** (bảng dưới). Chốt công thức: **KHÔNG cắt cảnh rời** — một khung máy DUY NHẤT suốt video, máy quay phóng và xoay liên tục (`perspective` + `rotateX/Y/Z` + hơi thở sin biên độ nhỏ), nội dung màn hình đổi bằng chồng mờ NGAY TRÊN MÀN; chuyển đoạn bằng chính cú xoay của máy (`rotateZ` 0→−90° để máy nằm ngang, dùng clip iOS quay ngang ở dạng GỐC chưa xoay rồi để khung máy xoay thay). Khung máy = ảnh mockup thật ở `_shared/📱 iPhone 17 Mockups (Community)/`, KHÔNG vẽ bằng CSS. Đúc từ video "Cơ hội tiềm năng" 04/08 (Thanh gạt bản đầu: *"không đạt, làm chi tiết như clip gốc, bạn không hề QA lại, frame điện thoại cũng tệ"*).
- 2026-08-04: **Thêm nhánh video giới thiệu tính năng app + 4 nguồn Remotion chính chủ.** Tách [`refs/REMOTION_RESOURCES.md`](refs/REMOTION_RESOURCES.md) (docs/resources · prompts · product-demo-for-presscut · showcase) kèm bảng chọn đường dựng **quay màn hình thật** vs **dựng lại giao diện bằng React** và nếp KFSP (mặc định quay thật, dựng React khi cần phóng to từng dòng hoặc thay số tránh lộ danh mục). Bối cảnh: Thanh đưa video mẫu AI gen 10 giây cho tính năng "Cơ hội tiềm năng" để làm lại bằng hình thật.
- 2026-07-29: **Video tách nhiều phần + căn frame bằng anchor whisper + tách phiên âm khỏi màn hình.** Thêm **L23** (🔴🔴 mỗi phần P1/P2 là video ĐỘC LẬP: tự đủ hook·teaser tính năng·CTA·spine, phần sau mở bằng recap 1-2 câu, teaser đặt ở quãng nghỉ giữa TỪNG phần với lời khác nhau), **L24** (🔴 anchor căn frame phải lấy từ **dump whisper segments**, không viết từ kịch bản — số đếm whisper ghi thành chữ số, tên riêng/đồng âm nghe lệch; script align cần normalize + forward search + fallback nửa anchor + in MISS, MISS còn sót thì không render; mẫu `scripts/align_p2.py`), **L25** (phiên âm CHỈ sống trong file TTS; trên màn luôn là thuật ngữ gốc EN viết đúng), **L26** (đoạn kết không có gì diễn ra → **card chữ thuần** 1 câu chốt, không pan lại chart cũ). Mục DESCRIPTION: bắt buộc mở `DESCRIPTION_YOUTUBE.md` bài trước làm khuôn (3 tiêu đề · caption ngắn · mô tả YT · hashtag · ghi chú GEO), video tách phần thì mỗi phần một khối trong cùng file. **Bỏ biến thể nobrand** theo CLAUDE.md rev28 (28/07) — chỉ còn một biến thể brand KFSP; Composition `Nobrand` giữ như di sản, không xuất bản. Đúc từ "Wyckoff Bài 4" (P1 2:49 + P2 2:09, 24-25/07).
- 2026-07-23: **Chèn clip QUAY MÀN HÌNH APP làm teaser tính năng (L22).** Thêm **L22** (🔴🔴 4 bẫy liên hoàn: clip đứng/nhảy vì map timeline tổng → bọc `<Sequence from>` cho t=0; `<Video>` fail render still → dùng `<OffthreadVideo>`; `cover` cắt méo → `contain` + card đúng tỷ lệ clip; muốn TO + không che → ẩn ProgressBar + tắt BrandOverlay `bottom` theo đoạn, disclaimer dòng riêng; compliance clip lộ mã/%lãi = organic + disclaimer). Kèm cách reuse ảnh Figma carousel PHA làm minh hoạ từng chặng (crop dải logo `ffmpeg crop=iw:ih-300:0:300` để không trùng/cắt logo video). Đúc từ "Wyckoff Bài 2" 22-23/07 (Thanh sửa 6 vòng độ to/tỷ lệ/che clip).
- 2026-07-19: **Biểu đồ tự-vẽ-dần theo giọng (L21) + camera giữ-yên-theo-câu.** Thêm **L21** (🔴🔴 cấm dùng 1 ảnh biểu đồ tĩnh pan camera cho cả video → phải dựng schematic ĐỘNG reveal-theo-beat, mỗi câu 1 phần tử diễn ra; phản-ví-dụ có khung riêng; ảnh Figma tĩnh chỉ làm hero/CTA/cover). Bổ sung nguyên tắc camera: giữ yên trong câu, snap-tại-onset (không lerp suốt segment) — motion chính đến từ nét vẽ. Đúc từ "Hộp chữ nhật Bài 2" 19/07 (Thanh gạt "1 biểu đồ dịch điểm đi dịch lại" + "hình chuyển động liên tục không theo nhịp giọng"). Tái dùng asset Figma có sẵn (`175:65`) + ảnh CTA Bài trước; đổi lời sau khi gen audio = gen lại (không splice).
- 2026-07-17 (b): **QA từng câu + bẫy đánh số ảnh tái dùng.** Thêm **L18** (🔴🔴 số in trên ảnh fanpage cũ chỏi số trong lời đọc → đổi số trên CLONE rồi export, nhất quán 4 chỗ giọng↔nhãn↔header↔caption), **L19** (caption dùng chung 2 beat → lệch lời), **L20** (card đè caption vì quên biên Ken Burns) + callout **QA TỪNG CÂU** (render still tại onset mọi beat, chia subagent soi song song, tự verify lại). Đúc từ "Hộp chữ nhật Bài 1" 17/07 — Thanh bắt *"minh hoạ 3 chiến lược đánh số bị nhầm, QA kỹ từng frame từng câu"*.
- 2026-07-17: **Lấy ảnh Figma = đường PLUGIN, không phải REST.** Tách [`refs/FIGMA_EXPORT.md`](refs/FIGMA_EXPORT.md) (runbook: server nhận ảnh + bẫy `allowedDomains` không khớp cổng + cắt vùng bằng page tạm `try/finally` + bảng sự cố cổng 9223/9224). Callout ASSET-FIRST điểm 2 bỏ cách base64→decode, trỏ sang runbook. Thêm L14-L17 (token REST hết hạn không chặn export; base64 ngốn token; chữ SVG tràn mép; chữ lặp caption). Đúc từ "Hộp chữ nhật Bài 1" 17/07 — Thanh chốt *"dùng plugin chứ sao lại qua api"*.
- 2026-07-03: **ASSET-FIRST cho video mẫu hình + căn giọng bằng silencedetect + crossfade.** Thêm L9–L13 (nến-code tệ→ảnh thật; hình lệch giọng→căn theo khoảng lặng thật; chớp đen→crossfade chồng cảnh; che mã→crop; spotlight vệt) + callout ASSET-FIRST (5 điểm) + kỹ thuật tách ảnh sạch từ Figma (exportAsync→base64→decode). Đúc từ "Hai đáy Bài 1" 03/07 (dựng lại thành công sau khi bản nến-code bị chê; Thanh nhấn: "không muốn nói đi nói lại chuyện minh hoạ tệ + hình lệch giọng").
- 2026-06-13: **Mục 📺 DESCRIPTION YOUTUBE (SEO + GEO).** Thêm template description + 2 checklist: SEO (keyword/chapters/hashtag/link) và GEO (Generative Engine Optimization — tóm tắt trả lời thẳng + FAQ Q&A + entity rõ để AI answer-engine trích dẫn) + geo-targeting VN. Kèm rule compliance + câu brand spine + biến thể nobrand/brand + ví dụ mẫu cho video "5 mẫu nến". (GEO ở đây = tối ưu cho bộ máy trả lời AI, không phải địa lý đơn thuần.)
- 2026-06-13: **Mục 🐞 LỖI THƯỜNG GẶP + chỉ dẫn đọc trước B5.** Ghi L1 (nút/tag nhảy 2 dòng → `whiteSpace: nowrap`), L2 (cảnh đối nghịch phải vẽ ngược chiều V↔Λ), L3-L5. Bước 5 thêm dòng "đọc LỖI THƯỜNG GẶP + render still QA trước full". Đúc từ video "5 mẫu nến đảo chiều đáy" (Thanh phát hiện nút Follow wrap + cảnh sắp giảm/sắp đáy vẽ giống nhau).
- 2026-06-13: **Bước 3-Gemini — gen hình hỗ trợ video.** Thêm sub-step sau GATE Bước 3: với asset `photo:`/`bg:` không phải screenshot app → sinh prompt Gemini đúng vân tay KFSP (neo vào `Brand/Image_Styles/kfsp_image_styles.json` + `KFSP_Image_Styles.md` + skill `kfsp-image-brief`). Có bảng map asset→kiểu hình (Kiểu 4 storytelling cho metaphor/cảm xúc...) + **6 khác biệt prompt cho video** (9:16; bg tông trầm chừa trống cho subtitle; no text/no logo; safe zone y150–1500; nến mã video `#34d399`/`#f87171`; nobrand không logo). Lý do: video "5 mẫu nến đảo chiều đáy" (13/06) cần ảnh quê chuồn chuồn/ráng mỡ gà cho hook → chuẩn hoá cách gen thay vì chế prompt ad-hoc.
- 2026-06-05: **Asset planning vào storyboard.** Bước 4 thêm cột `asset` (`code`/`photo:<file>`/`bg:<file>`) + sinh **Asset Manifest** (bảng hình cần Thanh cung cấp) trình ở GATE B4. Bước 3 đổi thành "thu thập theo manifest" (thứ tự đúng: B4 tính nhu cầu ảnh → B3 xin ảnh → B5 render). Template `PLAN_STORYBOARD_TEMPLATE.md` thêm mục 7b Asset Manifest. Lý do: video "Cái bẫy giá rẻ" (04-05/06) vẽ code hết rồi chèn ảnh See's/Berkshire/Munger giữa chừng → re-render nhiều lần. Đồng bộ QUICK REF ở engine CLAUDE.md (`~/Desktop/VIDEO KFSP/`).
- 2026-06-05: **Brand Frame chung** (mục "🟣 LỚP NHẬN DIỆN BRAND"). Đóng băng từ video "4 điểm vào lệnh": overlay gradient tím trên+dưới + logo trắng + brand tokens lấy TỪ APP (`#7B3AEC`/`#AA75FF`, bỏ magenta `#951B81` cũ). Component tái dùng `_shared/remotion/BrandFrame.tsx` (export `BrandOverlay`, `BrandLogo`, `BRAND`, `SAFE`). Brand-status theo kênh = chỉ khác có/không logo, overlay giữ ở cả 2. Tiêu đề có số → badge + tiêu đề thẳng hàng 1 dòng căn trái.

## 2026-08-06 — QA đổi từ "soi vài khung" sang "quét toàn bộ khung bằng máy"
Dự án "Cơ hội tiềm năng" bắt được **sáu lỗ khung** mà bộ khung soi chọn tay không thấy lỗ nào — tất cả rơi vào khe giữa hai khung được chọn. Luật QA ở `SKILL.md` đổi theo: dựng bản nháp `--scale=0.25` cả video rồi đo chênh lệch giữa hai khung liền và độ lệch trong từng khung, TRƯỚC khi soi still bằng mắt. Chi tiết + cách phân biệt lỗi với chuyển động thật: `LOI_RENDER.md` L73.
Thêm **L73 → L86** (14 mục): lớp lỗi "một khối tắt trước khi khối kế tiếp kịp lên", thứ tự vẽ vs mốc giờ, dốc chồng mờ phải mượt hai đầu, khoảng đẩy vật ra ngoài khung phải tính riêng từng chiều, tách trục cỡ và trục độ đậm cho hiệu ứng phủ dần, và luật "cú máy mà người duyệt phải hỏi nó là gì thì là nhiễu".

## 2026-08-06 (b) — Tách [`refs/HOOK_CHU.md`](HOOK_CHU.md): hook bằng chữ
Thanh yêu cầu tra cứu ngoài về kiểu hook chữ động rồi gom thành ref. Nội dung: **bảy kiểu chữ động có tên gọi** (Sequential Reveal · Enter & Exit · Create & Destroy · Morphing · Textures & Gradients · Flickering · Hypnotizing) kèm cái giá từng kiểu · **bốn mốc đo được** (hook ăn tiền trong 1–2 giây · 85% xem tắt tiếng · vùng an toàn 1080×1350 · trần một chỗ động mỗi đoạn) · **ba luật riêng của KFSP đúc từ bản dựng thật**: cỡ chữ phải đo bằng `fitText` chứ không ước bằng công thức 0,52 em (câu ngắn nhiều nét rộng thì ước lượng sai gấp rưỡi, chữ tràn ra ngoài khung) · kiểu tràn mép là mỗi dòng một cỡ để mép hai bên thẳng hàng · hai khối chữ khổng lồ KHÔNG được chồng mờ vào nhau mà phải một khối co lùi một khối ập vào. Kèm **bảng bốn trục** để tách tính từ "chưa ấn tượng" thành thứ đo được.
Đo thêm tại chỗ, tài liệu ngoài không có: **cột nút TikTok chiếm ~18% bề ngang bên phải ở tầm cao 45–85%**, nên chữ hook rộng 94% bề ngang bị che mấy chữ cuối; thu về 84% là hết.
